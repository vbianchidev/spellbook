import { AlphabeticOrdenationPipe } from './alphabetic-ordenation.pipe';

describe('AlphabeticPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphabeticOrdenationPipe();
    expect(pipe).toBeTruthy();
  });
});
